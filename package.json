{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "main",
      "sub-1",
      "sub-2"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
